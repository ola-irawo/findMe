import React from 'react'
import { ProfileCard, ProfileLayout } from '../features'

const UserProfile = () => {
  return (
    <section>
      <ProfileCard />
      <ProfileLayout />
    </section>
  )
}

export default UserProfile
